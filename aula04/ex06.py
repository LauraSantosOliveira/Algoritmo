salario_atual = float(input("Entre com o salário atual: R$ "))
aumento = float(input("Entre com o percentual de aumento: "))

print(f"Novo Salário: R$, {salario_atual + salario_atual * aumento:9.2f}")
